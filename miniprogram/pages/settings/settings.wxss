.settings__menus {
  padding: 0;
  overflow: hidden;
  border-radius: var(--radius-card);
}
.settings__menu--danger {
  --td-cell-title-color: var(--color-primary);
}
.settings__ver {
  text-align: center;
  font-size: 24rpx;
  margin-top: 32rpx;
}
